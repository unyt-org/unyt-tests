'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var ginlibsTypeCheck = require('ginlibs-type-check');

const compose = (...args) => {
  const funcs = args.reverse();
  let result = void 0;
  return function (arg) {
    result = arg;

    for (const it of funcs) {
      if (!ginlibsTypeCheck.isFunc(it)) {
        continue;
      }

      result = it(result);
    }

    return result;
  };
};
const sleep = time => {
  return new Promise(resolve => {
    setTimeout(resolve, time);
  });
};
const randomStr = (len = 23) => {
  let str = Math.random().toString(36).substring(2, 15);

  while (str.length < len) {
    str += Math.random().toString(36).substring(2, 15);
  }

  return str.slice(0, len);
};

exports.compose = compose;
exports.randomStr = randomStr;
exports.sleep = sleep;
